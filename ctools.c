#include <stdio.h>

float brightness(int* arr, int width, int height) {
    //return *(arr + height * 3 + width * height * 3);
    int x;
    int y;
    int a;
    for (x = 0; x < 9000; x ++) {
        for (y = 0; y < 10000; y ++) {
            a += 1;
        }
    }
    return 1.0;
}